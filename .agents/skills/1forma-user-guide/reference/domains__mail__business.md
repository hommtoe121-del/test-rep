# Почта

Источник: https://help.1forma.ru/domains/mail/business/

Бизнес-описание встроенного почтового клиента 1Формы: серверы IMAP/POP3/EWS, ящики, клиент, привязка к категориям, почтовые смарты и шаблоны уведомлений. Для администраторов и внедренцев: настройка приёма/отправки, маршрутизация писем в задачи.

В 1Форму встроен полноценный почтовый клиент, интегрированный с задачами и автоматизацией. Система подключается к внешним почтовым серверам по протоколам IMAP, POP3 и через EWS (Exchange Web Services), обеспечивая приём, отправку и синхронизацию писем. Письма сохраняются в системе и могут быть привязаны к задачам: автоматически (через привязку ящика к категории или почтовые смарты) или вручную. Поддерживаются почтовые провайдеры mail.ru, yandex.ru, gmail.com, Microsoft Exchange, а также собственные IMAP/SMTP-серверы.
 Ключевые возможности:

- Встроенный почтовый клиент в навигационной панели пользователя

- Автоматическое создание задач из входящих писем

- Почтовые смарт-правила для маршрутизации входящей корреспонденции

- Шаблоны оформления для всех типов системных уведомлений

- Вынесение резолюций по подписям прямо из email-уведомлений

Почтовый сервер — это подключение к внешнему mail-серверу с заданными параметрами IMAP/SMTP. Администратор регистрирует серверы в разделе "Система - Почта", указывая адреса и порты IMAP/SMTP, параметры SSL/TLS (рекомендуется TLS 1.2), тип провайдера и дополнительные настройки.

При регистрации сервера администратор выбирает тип подключения:

- Стандартный — фоновая синхронизация по расписанию

- Онлайн-IMAP — синхронизация в реальном времени (требуется отключить флаг «Синхронизируется» в ящике)

-  Exchange — для Microsoft Exchange; рекомендуется вместо IMAP+Exchange, так как поддерживает фильтрацию, сортировку и русскоязычный поиск


-  Exchange Server — флаг принадлежности к MS Exchange (зарезервирован; принудительно проверяет доступ по IMAP)


- Gmail — флаг принадлежности к Google Mail

- CalDav — интеграция с CalDav-календарём (адрес, провайдер: Kerio / Default)

- EWS URL — адрес Exchange Web Services

- Анонимная аутентификация по SMTP — отправка без авторизации (для ящиков "только для отправки")

- Разрешённые почтовые домены — домены, с которых допустима подмена адреса отправителя (через запятую)

- Ошибки прекращения обработки — список ошибок, при которых обработка письма останавливается и система переходит к следующему
  Протокол MAPI не поддерживается напрямую — для работы с ним используется промежуточный обработчик. Технические детали подключения и используемые почтовые библиотеки описаны в admin.md.



Системный почтовый ящик предназначен для отправки уведомлений платформы (создание задач, смена статусов, комментарии и т.д.). Настраивается в разделе "Настройки почты" через опцию "Использовать почтовый ящик для системных писем". Также должна быть активна опция "Отправлять письма от имени системного ящика" в общих настройках приложения.
 Рекомендации для системных ящиков:

- Хранить письма 5-15 дней (чтобы устаревшие уведомления не накапливались)

- Синхронизировать письма за 5-15 дней

- Не использовать для повседневной переписки
  В общих настройках приложения определяются два специальных системных ящика:

- Почтовый ящик для ответов — принимает ответы пользователей на уведомления и регистрирует их как комментарии к задачам

- Внешний почтовый ящик для ответов — аналог для пользователей без доступа к внутренней сети
  Для корректной работы ответов необходимо создать системную категорию "Почта" в разделе "Системный" и подключить к ней соответствующий ящик.

Каждому пользователю может быть предоставлен доступ к почте (флаг "Доступ к почте" в профиле или через список в настройках почтового клиента). После этого в навигационной панели появляется вкладка "Почта", где пользователь может добавлять внешние почтовые ящики (Gmail, Yandex, корпоративные и др.).
 Пользовательские ящики не должны быть синхронизируемыми — при активной синхронизации письма сохраняются в системе, что при большом потоке ведёт к переполнению хранилища. Отправка автоматических писем на эти ящики через смарт-действия при этом работает.

Основные параметры почтового ящика:

- Почтовый сервер — привязка к зарегистрированному серверу

- Название — отображаемое имя на вкладке "Почта"

- Имя и email отправителя — подставляются в исходящие письма

- Логин / Пароль — учётные данные; логин заполняется автоматически

- Логин / Пароль CalDav — отдельные учётные данные для CalDav

- Использовать только для отправки — отключает приём входящих

- Хранить письма N дней — автоочистка (0 = хранить всегда; для системных ящиков рекомендуется 5-15 дней)

- Синхронизировать письма за N дней — глубина синхронизации

- Количество неудачных попыток синка — порог, после которого ящик исключается из синхронизации; счётчик можно сбросить вручную

- Временно отключить — альтернатива удалению

- Не использовать в смартах — скрывает ящик из списка доступных в смарт-действии "Отправить email"

- Не формировать адресную книгу — отключает список контактов при создании письма

- Синхронизируется — должен быть включён только для служебных ящиков, работающих с почтовыми смартами

- Включить события о новых письмах (Exchange) — активирует глобальное смарт-событие «Пришло новое письмо»

При настройке ящика можно выбрать, какие папки синхронизировать и к какой папке на сервере привязать каждую системную папку 1Формы:

- Входящие — папка для полученных писем

- Черновики — папка для неотправленных писем

- Отправленные — папка для отправленных писем

- Удалённые (Корзина) — папка для удалённых писем

- Архив — папка для архивных писем

- Спам — папка для нежелательной почты
  Опция "Синхронизировать все папки" подключает все созданные в ящике папки. Для каждой из основных системных папок явно указывается соответствующая папка на сервере (по умолчанию — стандартные папки IMAP: INBOX, Drafts, Sent, Trash/Junk).
 К каждому ящику назначаются пользователи (видят ящик на вкладке "Почта") и владельцы (дополнительно могут менять настройки ящика).

Встроенный почтовый клиент доступен на вкладке «Почта» в навигационной панели и по возможностям близок к привычным почтовым программам (только веб-версия). В мобильных приложениях функциональность ограничена: на iOS доступно только чтение почты, на Android — чтение и отправка. Пользователь может:

- Читать входящую и исходящую почту из подключённых ящиков

- Писать и отправлять письма

- Создать задачу из письма — превращает входящее письмо в задачу

- Связать письмо с существующей задачей — привязка к уже созданной задаче

- Написать письмо из задачи — отправка письма в контексте задачи

 Все письма, получаемые и отправляемые через встроенный клиент, сохраняются в системе: тема, тело (в виде HTML и текста), исходник с сервера и сопутствующие данные; вложения хранятся отдельно. Это касается как синхронизируемых ящиков, так и ящиков с онлайн-доступом по IMAP/Exchange — в последних список и просмотр подтягиваются с внешнего сервера в реальном времени, но письма всё равно сохраняются. Срок хранения ограничивается параметром ящика «Хранить письма N дней» (0 = хранить всегда); устаревшие письма удаляются автоматически по расписанию.
 Текстовая версия тела письма. Если во входящем письме есть текстовая часть (`text/plain`), она используется как текст письма. Если письмо собрано только как HTML (без текстовой альтернативы — например, при отправке через веб-интерфейс почтового сервиса), текстовая версия автоматически формируется из HTML-тела. Поэтому текст письма — и «Текст обращения» в созданной задаче — заполнен всегда.

Администратор может ограничить частоту отправки писем (количество писем в минуту) для соответствия корпоративным политикам и противодействия спаму. Пользователю может быть предоставлено право самостоятельно определять правила синхронизации своего ящика (через настройки индивидуальных джобов).
 Контекстное меню почтового ящика предоставляет пользователю дополнительные операции:
 Пункт меню Описание Новое письмо Создать новое письмо для отправки из данного ящика Настройки Управление настройками ящика. Доступно администраторам ящика Шрифты и подписи Выбор шрифтов и добавление автоматической подписи к исходящим сообщениям Добавить папку поиска Создать виртуальную папку для поиска писем по заданным критериям Добавить ящик Подключение нового почтового ящика Удалить Удаление ящика (доступно владельцам)
 Индикатор непрочитанных писем отображается над вкладкой «Почта» в боковой панели. При наличии нескольких подключённых ящиков показывается суммарное количество.
 Действия с папкой: В избранное, Отметить все как прочитанные, Очистить папку.

 Индикаторы рядом с системными папками (Входящие, Черновики, Удалённые) — количество писем. При ошибке авторизации в ящик отображается красный индикатор с кнопкой «Войти» для повторной авторизации.

Добавить ящик можно с пустой вкладки «Почта» (кнопка «Создать»), либо при наличии уже подключённых — кнопкой `+` или через пункт «Добавить ящик» в контекстном меню. Открывается 4-шаговый мастер; для перехода на следующий шаг — `Enter` или кнопка «Далее».

 Шаг 1. Вход в почту:
 Поле Описание Почтовый сервер Выбор из списка. При нестандартном — обратиться к администратору Логин Учётные данные для доступа к почтовому серверу. Заполняется автоматически Пароль Пароль от почтового сервера, указанного в поле «Почтовый сервер». Не является паролем от учётной записи в системе «Первая Форма» Логин CalDav / Пароль CalDav Учётные данные для доступа к почтовому серверу CalDav. Появляются при выборе сервера CalDav Адрес CalDav Если почтовый сервер имеет адрес CalDAV, поле заполняется автоматически. Если адрес CalDav пустой — настройка обязательна для добавления ящика на этом шаге. Появляется при выборе сервера CalDav
 Шаг 2. Настройка ящика:
 Поле Описание Название почтового ящика Имя на вкладке «Почта». Макс. 25 символов Имя отправителя Будет в поле «От» Email отправителя По умолчанию — значение из профиля пользователя Владельцы ящика Сотрудники, у которых ящик доступен и которые могут менять его настройки Другие пользователи ящика Сотрудники, у которых ящик появится во вкладке «Почта» (без прав на настройки)
 Шаг 3. Папки для синхронизации: для каждой системной папки 1Формы указывается соответствующая папка в почтовом ящике.
 Поле Описание Синхронизировать все папки Флаг синхронизирует все созданные в почтовом ящике папки Входящие Выбор папки для входящих писем Черновики Выбор папки для писем, которые не были отправлены Отправленные Выбор папки для отправленных писем Удалённые (Корзина) Выбор папки для удалённых писем Архив Выбор папки для архивных писем Спам Выбор папки для нежелательной почты
 Шаг 4. Дополнительные настройки:

- Использовать в смартах — ящик отображается в списке доступных у смарт-действия «Отправить email». Доступно только администратору.

- Отображать количество непрочитанных писем в меню — включает индикатор над вкладкой «Почта» в боковой панели (сумма по всем подключённым ящикам).

Пароли почтовых ящиков хранятся в зашифрованном виде. В списке ящиков пароль не отображается и доступен только в форме редактирования. Подробности о механизме шифрования и его учёте при ИБ-аудите — в admin.md и support-guide.md § 3.0.
 В списке писем поддерживается:

- поиск по тексту, отправителю и теме;

- сортировка по колонкам;

- фильтрация и группировка.
  Поиск писем на русском языке не работает для почтового сервера EWS с доступом по IMAP. Это ограничение протокола IMAP при подключении к Exchange-серверу — для полнотекстового поиска рекомендуется использовать нативный доступ EWS.

Помимо почтового клиента (вкладка "Почта"), пользователь может отправить письмо из нескольких мест интерфейса.
 Из карточки задачи. В меню «Документы» на панели инструментов карточки задачи доступна кнопка «Написать письмо». При её нажатии открывается форма создания письма, где поле «От кого» заполнено автоматически — подставлен почтовый ящик текущего пользователя.
 Из профиля пользователя. При наведении курсора на адрес электронной почты на вкладке «Информация» в профиле пользователя появляются две кнопки: скопировать адрес в буфер обмена и открыть форму создания письма (если у текущего пользователя подключён почтовый ящик). Если ящик не подключён, кнопка открывает почтовое приложение устройства.
 Из справочника сотрудников. При нажатии на адрес электронной почты в колонке «Контакты» справочника сотрудников открывается форма нового письма с предзаполненным полем получателя (при наличии у текущего пользователя подключённого почтового ящика).

Форма создания письма содержит стандартные поля, опции для вложений и запрос уведомления о прочтении:
 Поле Описание От кого Отображает текущий активный почтовый ящик. При нескольких подключённых ящиках нужный выбирается из выпадающего списка Кому Обязательное поле. Адрес получателя письма Копия Необязательное поле. Адреса получателей копии письма СК Необязательное поле. Скрытая копия — адреса получателей, которые не видны остальным получателям Тема Обязательное поле. Краткое описание содержания письма Текст письма Основное содержание письма. Поддерживается форматирование Вложения Прикреплённые к письму файлы Уведомление о прочтении Флаг запроса подтверждения прочтения

При выборе письма в списке оно отображается снизу или справа от списка — в зависимости от персональных настроек. Если отправитель — пользователь системы, клик по его имени открывает мини-карточку.
 Кнопки в шапке письма:
 Действие Поведение Ответить Форма нового письма с цитатой; в «Кому» — отправитель Ответить всем Форма с цитатой; в «Кому» — отправитель и все адресаты исходного Переслать Форма с цитатой; «Кому» и «Копия» пустые Удалить Удалить письмо из папки Предыдущее / Следующее Навигация по списку Свернуть / Развернуть Развернуть письмо на весь экран Безопасность отображения письма. Тело входящего письма очищается на сервере перед показом: форматирование (таблицы, списки, шрифты, цвета) и встроенные картинки сохраняются, а исполняемый код из письма удаляется. Ссылки из письма открываются в новой вкладке. Если тело не удалось очистить, вместо письма показывается ошибка — так исключается выполнение кода из письма внешнего отправителя.
 В меню «Ещё»:

- Пометить как непрочитанное — увеличивает счётчик непрочитанных и помечает письмо жирным шрифтом в общем списке.

- Создать задачу и вложить письмо — см. «Привязка письма к задаче» ниже.

Если в письме есть вложения, они отображаются в отдельном блоке. По умолчанию он свёрнут до 2 файлов, кнопка «Развернуть» показывает полный список. Файлы содержимого письма (изображения из текста) во вложениях не отображаются.

 По клику файлы открываются во встроенном просмотрщике; офисные документы — в R7/OnlyOffice (см. files/business.md).

Связь между письмом и задачей можно установить тремя способами:

- Из почтового клиента, привязка к существующей задаче — кнопкой на панели инструментов открытого письма. Если письмо только что отправлено, оно сначала попадает в папку «Исходящие», а затем (после синхронизации с сервером) перемещается в «Отправленные». Связь можно установить, не дожидаясь этого перемещения — открыв письмо из «Исходящих»; после попадания в «Отправленные» связь сохраняется.

- Из почтового клиента, создание новой задачи — действие «Создать задачу и вложить письмо» в карточке письма открывает выбор категории, затем карточку создания задачи. После сохранения письмо в формате `.eml` появляется во вкладке «Письма» в блоке «Файлы» задачи.

- Из карточки задачи — кнопка в панели блока «Файлы» открывает папку «Входящие» во встроенном почтовом клиенте; выбранное письмо привязывается к задаче и попадает на вкладку «Письма».

 Привязанное письмо появляется в задаче — в блоке «Файлы» на вкладке «Письма».

 Письмо в формате `.eml` из карточки задачи открывается во встроенном просмотрщике без скачивания.

 Если у входящего письма сохранён оригинальный MIME-источник, при вложении в задачу через смарт-действие «Создать задачу» с опцией «Вложить письмо файлом» он отдаётся как есть — `.eml` содержит все оригинальные транспортные заголовки (`Return-Path`, `Received`, `Message-Id`, `DKIM-Signature`, `Received-SPF`) и корректную MIME-структуру тела (HTML остаётся `text/html`, не превращается в `text/plain` с base64). Такой файл нормально открывается во внешних почтовых клиентах (Thunderbird, Outlook) с оригинальным форматированием. Для писем без сохранённого источника (исходящие либо старые входящие, у которых исходник не сохраняли) `.eml` собирается из модели письма — заголовки фиксированы базовыми (`From`, `To`, `Subject`, `Date`), но тело корректно объявляется как HTML, если у письма есть HTML-версия. Поведение действует с 2.268.356; подробности параметра — в `smart-actions/actions-reference.md`.

К категории может быть подключён выделенный почтовый ящик. При поступлении письма на этот ящик автоматически создаётся задача в категории от имени отправителя. Поля письма переносятся в задачу так:

- Кому — становятся исполнителями задачи (если есть в системе)

- Копия — становятся подписчиками

- Тело письма — текст задачи / комментарий (опционально и то, и другое)

- Вложения — файлы задачи

- Комментарии из задачи автоматически отправляются отправителю письма
  Форматирование письма. Форматирование входящего письма (цвет, шрифты, таблицы) при записи в текст задачи или комментарий не сохраняется — тело письма отображается как обычный текст.
 Важное ограничение: ящик, подключённый к категории, не должен быть закреплён за пользователем.

При подключении ящика к категории настраиваются:

- Протокол: IMAP или POP3

- Папка ящика, из которой берутся письма (например, «Входящие»)

- Количество писем для загрузки за сессию (по умолчанию 100)

- Дублирование тела письма в текст задачи

- Автоматическое создание пользователей из поля «Кому» (новые попадают в группу для внешних пользователей)

- Обработка авто-ответов (ответы становятся комментариями, а не новыми задачами)

- Вложение оригинала письма

- Дополнительная обработка после загрузки всех писем (настраивается администратором, см. admin.md)

По метке в теме письма система определяет, что с ним делать:

- Тема без метки — создаётся новая задача

- Метка с номером задачи — письмо записывается в существующую задачу

- Метка с номером комментария — письмо становится ответом на этот комментарий
  Сам текст метки (по умолчанию `[1f]`) задаётся в общих настройках как «Краткое имя приложения». Точные форматы меток — в admin.md.
 `flowchart TD
    A["Входящее письмо в ящик, подключённый к категории"] --> G{"Авто-ответ?"}
    G -- "да" --> H["Регистрируется комментарием, не новой задачей"]
    G -- "нет" --> B{"Метка в теме письма?"}
    B -- "нет метки" --> C["Новая задача в категории"]
    B -- "номер задачи" --> D["Запись в существующую задачу"]
    B -- "номер комментария" --> E["Ответ на этот комментарий"]
    C --> F["Поля письма → задача: Кому → исполнители, Копия → подписчики, тело → текст, вложения → файлы"]` Тело письма может содержать специальные теги для автоматического заполнения дополнительных параметров задачи. Для этого в настройках категории должен быть отключён HTML. Формат тегов описан в admin.md.
 На уровне категории можно управлять почтовыми уведомлениями:

- Полностью отключить отправку почтовых сообщений (при этом уведомления продолжают отображаться в ленте)

- Добавить значение ДП или произвольный текст в заголовок писем

- Указать отдельный ящик и имя отправителя для внешних пользователей

- Задать текст письма об отклонении

Почтовые смарты позволяют автоматически обрабатывать входящие письма по условиям без выделения отдельного ящика на каждую категорию. Настраиваются в разделе «Почта — Smart» и привязываются к конкретной папке конкретного ящика.
 Обязательное условие: у ящика должна быть включена опция «Синхронизируется».

Правила работают аналогично категорийным смартам: при поступлении письма в указанную папку система анализирует тему, содержание, адрес отправителя и другие атрибуты. Если условия выполнены, срабатывает пакет действий.
 Пакет действий, привязанный к папке почтового ящика, исполняется в почтовом контексте события. Это отличает его от пакета, привязанного к категории задач. При создании или редактировании смарт-выражения внутри такого пакета (например, для значения ДП в действии «Создать задачу») в дереве сущностей доступны почтовые параметры входящего письма: отправитель, получатели, тема, тело, дата получения, вложения и др. Кроме новых выражений, в списке выбора отображаются ранее созданные почтовые смарт-выражения — их можно переиспользовать. Почтовый контекст появляется автоматически при входе в редактор из пакета, привязанного к ящику; отдельной настройки не требуется. В категорийных пакетах почтовый контекст недоступен — там используется контекст задачи.

Стандартные:

- Создать задачу

- Привязать письмо к задаче

- Переместить письмо в папку

- Перейти в заданный статус / сделать переход / принудительно изменить статус

- Изменить значение ДП / массово изменить ДП / копировать ДП / очистить ДП

- Отправить email / email на группу / системный email / SMS

- Вложить файл

- Создать / уволить пользователя

- Назначить / удалить заместителя

- Обновить расширенное свойство пользователя
  Дополнительные (из подключённых расширений):

- Синхронизировать дополнительные параметры задач
  Один ящик для входящих обращений, письма сортируются по теме:

- Тема содержит «Обращение» — создаётся задача в категории «Обращения граждан», текст письма копируется в текст задачи, адрес отправителя записывается в ДП

- Тема содержит «Заявка» — создаётся задача в другой категории
  Если в категории настроена постановка задач из почты, изображения в теле письма вставляются как картинки (при разрешённом HTML) или вырезаются (если HTML запрещён).

Шаблоны почтовых уведомлений задают оформление системных писем. Каждый шаблон привязан к определённому событию и может быть ограничен конкретной категорией и языком интерфейса.

Шаблоны можно создать примерно для 40 типов событий. Основные группы:

- Задачи: создание задачи, смена статуса (в том числе принудительная), изменение текста, изменение и просрочка срока

- Исполнители: назначение и снятие исполнителя или ответственного, отказ от задачи

- Комментарии: новый комментарий, редактирование комментария (в обычном и упрощённом виде)

- Подписи и акцепт: назначение подписанта, согласование и отклонение подписи (в том числе динамической), приёмка задачи — с вариантами для эскалации и делегирования

- Пользователи: приглашение, новый пароль, восстановление пароля, добавление и снятие заместителя

- Дополнительные параметры: изменение значений ДП

- Прочие: добавление подписчика, отказ в доступе, обновление описания файла, отсутствие сотрудника, уведомления помощнику

- Встречи (Exchange): уведомление участникам Exchange-встречи при её создании; отдельный вариант добавляет в письмо блок «Перейти к задаче», если встреча связана с задачей

Шаблон состоит из темы письма, тела (с таблицей и оформлением) и стилей (шрифты, цвета шапки, кнопок и ссылок). В шаблон можно вставлять данные задачи (номер, текст, статус, исполнитель и др.), готовые надписи на нужном языке («Новая задача», «Исполнитель», «Заказчик») и интерактивные элементы — ссылку на задачу, путь к категории, значения ДП, кнопки резолюций по подписи, ссылку отписки и вложения. Полный список доступных элементов и формат разметки — в admin.md.
 В email-уведомлениях (назначение исполнителем, новый комментарий, смена статуса) есть кнопка «Отписаться». По ней пользователь попадает на страницу задачи, где может отписаться кнопкой «Отписаться» в шапке задачи. Текст кнопки корректно переводится на все языки интерфейса.

При отправке используется язык интерфейса получателя. Приоритет выбора шаблона: 1. Шаблон для языка получателя 2. Шаблон, помеченный как «международный» 3. Шаблон по умолчанию
 Шаблон может быть привязан к конкретной категории (например, отдельный дизайн для определённого типа задач) или быть общим.
 В письмах с запросом подписи есть кнопки для вынесения резолюции. Поведение кнопок зависит от настройки администратора:

- Открытие в браузере — кнопка открывает интерфейс 1Формы, где пользователь выносит резолюцию.

- Ответ письмом — кнопка открывает почтовый клиент с подготовленным ответом; система обрабатывает его автоматически и регистрирует резолюцию, а текст ответа сохраняет как комментарий к ней.

Ответ на уведомление по задаче автоматически публикуется как комментарий в этой задаче — система распознаёт задачу по метке в теме письма. Если ответ относится к конкретному комментарию, он становится ответным комментарием. Если письмо не привязано ни к какой задаче, в привязанной категории создаётся новая задача.
 Шаблоны можно выгрузить в файл (в том числе только общие, без привязки к категории) и загрузить обратно. При импорте шаблоны добавляются в конец списка; можно сразу их включить и/или сохранить существующие шаблоны с такими же именами.

Приём, отправка, синхронизация и очистка писем выполняются в фоне по расписанию — отправка письма или появление входящего происходят не мгновенно, а в ближайшем цикле обработки. Для надёжности проблемные письма (с многократными ошибками) обрабатываются отдельно, чтобы не задерживать остальную очередь. Подробно о фоновых заданиях и их настройке — в admin.md.
 Администратор может просматривать очередь отправки (кнопка «Очередь на отправку» на странице почты): дату постановки письма, число неудачных попыток и текст ошибки; очередь можно очистить целиком. Журнал почтовых заданий (кнопка «Логи почтовых джобов») фильтруется по периоду, типу задания, наличию ошибок и тексту сообщения и показывает, сколько писем обработано успешно и с ошибками.

Специальный тип дополнительного параметра для выбора email-адресов пользователей системы. Список выбора можно ограничить: без ограничений (все работающие сотрудники), по группе или по смарт-выражению (возвращает список пользователей). Подписи кнопки и подсказки настраиваются.

Почтовый модуль интегрирован с задачами и подписями: входящие письма могут порождать задачи, ответы на уведомления регистрируются как комментарии, а резолюции по подписям можно выносить прямо из email.

- Создание задачи из входящего письма (через привязку ящика к категории или почтовый смарт)

- Привязка письма к существующей задаче из почтового клиента

- Отправка письма из контекста задачи

-  Ответы на уведомления регистрируются как комментарии


-  Подключение почтового ящика к категории для автоматического приёма заявок


- Настройка почтовых сообщений на уровне категории (вкл/выкл, заголовки, ящик для внешних)

-  Привязка шаблонов уведомлений к конкретным категориям


-  Шаблоны уведомлений о запросе подписи (назначение подписанта и варианты)


- Вынесение резолюций прямо из email — в браузере или ответом письмом

- Автоматическая обработка ответных писем с резолюциями

Смарт-автоматизация охватывает всю цепочку работы с почтой: фильтрацию входящих по условиям, отправку исходящих действиями, заполнение ДП из тела письма и управление доступом пользователей.

- Почтовые смарты — автоматическая обработка входящих писем по условиям

- Глобальное смарт-событие «Пришло новое письмо» (Exchange)

-  Смарт-действия «Отправить email» / «Отправить email на группу» / «Отправить системный email»


-  ДП типа «Адресаты Email»


- Заполнение ДП из тела письма

-  Добавление значения ДП в заголовки уведомлений


-  Управление доступом к почте (флаг в профиле)


- Автоматическое создание внешних пользователей из входящих писем

-  Назначение пользователей и владельцев на почтовые ящики


-  Интеграция с CalDav через настройки почтового сервера


-  Синхронизация с Exchange-календарём (счётчик неудачных попыток)


-  Системная категория «Уведомления о прочтении» с подключённым ящиком


- Шаблоны уведомлений для примерно 40 типов событий

- Настройка отправки: от имени системного ящика или с подменой адреса (в рамках разрешённых доменов)

Письма хранятся в `Emails` (привязаны к ящику `EmailMailBoxes`). Получатели письма — `EmailRecipients`; раскладка по папкам — `EmailMailBoxesFoldersEmails` (папки `EmailMailBoxesFolders` образуют дерево через `ParentFolderId`); связь письма с задачей — `EmailTaskLinks` (плюс прямое поле `Emails.TaskId`). Доступ пользователей к ящикам задаёт `EmailMailBoxesUsers`, адресные книги — `EmailAddressBooks` и `EmailAddressBooksEmails`.
 `graph LR
    EM["Emails — письма"]
    MB["EmailMailBoxes — почтовые ящики"]
    MBU["EmailMailBoxesUsers — ящики пользователей"]
    MBF["EmailMailBoxesFolders — папки ящиков"]
    MBFE["EmailMailBoxesFoldersEmails — письмо в папке"]
    ER["EmailRecipients — получатели"]
    ETL["EmailTaskLinks — связь с задачей"]
    AB["EmailAddressBooks — адресные книги"]
    ABE["EmailAddressBooksEmails — адресат письма"]
    T["Tasks — задачи"]
    U["Users — пользователи"]

    EM -- "MailBoxID" --> MB
    EM -- "TaskId" --> T
    EM -- "CreatorID (создатель)" --> U
    MBU -- "MailBoxID" --> MB
    MBU -- "UserID" --> U
    MBF -- "MailBoxID" --> MB
    MBF -- "ParentFolderId" --> MBF
    MBFE -- "FolderId" --> MBF
    MBFE -- "EmailId" --> EM
    ER -- "EmailId" --> EM
    ER -- "UserId" --> U
    ETL -- "TaskId" --> T
    ETL -- "EmailId" --> EM
    AB -- "MailBoxId" --> MB
    ABE -- "EntryId" --> AB
    ABE -- "EmailId" --> EM` Часть связей на уровне БД не закреплена внешними ключами (создатель `Emails.CreatorID`, получатели `EmailRecipients`, адресные книги) — соответствие поддерживается приложением; на схеме они показаны как логические.
 Полная версия с колонками и схемы связей других подсистем БД — в system/db-er-diagrams.md.
